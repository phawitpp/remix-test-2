import React from "react";

interface Props {
  // Define your component props here
}

const FormsIndex: React.FC<Props> = () => {
  // Add your component logic here

  return (
    <div>
      <span>Select a form from the sidebar</span>
    </div>
  );
};

export default FormsIndex;
